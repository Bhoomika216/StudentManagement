
@if (User.Identity.IsAuthenticated)
{
    <form method="post" class="form-inline" asp-page="/Account/Logout">
        Welcome @User.Identity.Name
        <button type="submit" class="m-lg-2 btn btn-secondary btn-sm active btn-outline-dark text-white bg-dark" role="button" aria-pressed="true">Logout</button>
    </form>
}
else
{
    @*<a class="m-lg-2 btn btn-secondary btn-sm active btn-outline-dark text-white bg-dark" asp-page="/Account/Login">Login</a>*@
}